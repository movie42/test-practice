import { render, screen } from "@testing-library/react";

describe("", () => {
  const setup = (props = {}) => {
    return {
      ...utils,
    };
  };

  it("", () => {
    const { input, button } = setup();
  });
});
